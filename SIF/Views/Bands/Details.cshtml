
@model SIF.Models.Band

@{
    ViewData["Title"] = "Detalles";
}

<h1>Detalles</h1>

<div>
    <h4>Band</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.BandName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.BandName)
        </dd>
    </dl>
</div>

<div>
    <a asp-action="Edit" class="btn btn-outline-warning" asp-route-id="@Model.BandId">Edit</a>
    <a asp-action="Index" class="btn btn-outline-dark">Go Back</a>
</div>